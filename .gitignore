# Upstream checkouts — pinned by SHA in env/versions.env, never committed in place.
vendor/
auto/logs/

# Maven build output for demos with their own pom.xml.
target/

# Internal prep notes for the NVIDIA sync — not for the public repo.
.claude/

# Compiled demo output (built into a temp dir by the run scripts).
*.class
